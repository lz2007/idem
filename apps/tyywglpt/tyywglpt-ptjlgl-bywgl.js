import avalon from 'avalon2';
import ajax from '../../services/ajaxService';

import {
    createForm,
    message,
    notification
} from 'ane';

export const name = 'tyywglpt-ptjlgl-bywgl';

//页面组件
avalon.component(name, {
    template: __inline('./tyywglpt-ptjlgl-bywgl.html'),
    defaults: {

    }
});